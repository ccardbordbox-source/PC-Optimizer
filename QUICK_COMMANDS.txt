════════════════════════════════════════════════════════════════════
  QUICK COMMAND SHEET - GitHub Upload
  Copy and paste these commands in PowerShell
════════════════════════════════════════════════════════════════════

STEP 1: Navigate to project folder
────────────────────────────────────────────────────────────────────
cd C:\Users\SneakyBloop\Desktop\test


STEP 2: Initialize Git
────────────────────────────────────────────────────────────────────
git init


STEP 3: Configure Git (first time only)
────────────────────────────────────────────────────────────────────
git config user.name "YOUR_NAME"
git config user.email "YOUR_EMAIL@example.com"

⚠ Replace YOUR_NAME and YOUR_EMAIL with your info!


STEP 4: Add files
────────────────────────────────────────────────────────────────────
git add .


STEP 5: CRITICAL - Check what will be uploaded
────────────────────────────────────────────────────────────────────
git status

⚠ VERIFY: pc_optimizer_v2.py should NOT be in the list!
✅ If not listed → Safe to continue
❌ If listed → STOP! Check .gitignore


STEP 6: Commit
────────────────────────────────────────────────────────────────────
git commit -m "Initial release v2.2.0 - Documentation and build tools"


STEP 7: Add remote (replace YOUR_USERNAME!)
────────────────────────────────────────────────────────────────────
git remote add origin https://github.com/YOUR_USERNAME/PC-Optimizer.git

⚠ Replace YOUR_USERNAME with your GitHub username!


STEP 8: Push to GitHub
────────────────────────────────────────────────────────────────────
git branch -M main
git push -u origin main


════════════════════════════════════════════════════════════════════
  FUTURE UPDATES - Run these when you make changes
════════════════════════════════════════════════════════════════════

Update documentation:
────────────────────────────────────────────────────────────────────
git add .
git commit -m "Update documentation"
git push


Update version.json (after creating new release):
────────────────────────────────────────────────────────────────────
git add version.json
git commit -m "Update to v2.3.0"
git push


════════════════════════════════════════════════════════════════════
  TROUBLESHOOTING
════════════════════════════════════════════════════════════════════

If you accidentally added pc_optimizer_v2.py:
────────────────────────────────────────────────────────────────────
git rm --cached pc_optimizer_v2.py
git commit -m "Remove source file"
git push


If you need to start over:
────────────────────────────────────────────────────────────────────
rmdir /s /q .git
git init
# Then start from STEP 3 above


Check what's in .gitignore:
────────────────────────────────────────────────────────────────────
type .gitignore


Verify remote URL:
────────────────────────────────────────────────────────────────────
git remote -v


════════════════════════════════════════════════════════════════════
  VERIFICATION
════════════════════════════════════════════════════════════════════

After uploading, check these URLs in browser:

Your repository:
https://github.com/YOUR_USERNAME/PC-Optimizer

Raw version.json:
https://raw.githubusercontent.com/YOUR_USERNAME/PC-Optimizer/main/version.json

⚠ Replace YOUR_USERNAME!

════════════════════════════════════════════════════════════════════

Copy this entire file and keep it handy for reference! 📋
